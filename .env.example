# Variables de entorno para Flow (API de pagos de Chile)

# Credenciales de Flow
# Obtén estas credenciales desde tu cuenta en Flow: https://www.flow.cl (producción) o https://sandbox.flow.cl (sandbox)
FLOW_API_KEY=tu_api_key_aqui
FLOW_SECRET_KEY=tu_secret_key_aqui

# Configuración del entorno Flow
# Usa "sandbox.flow.cl" para pruebas y "www.flow.cl" para producción
FLOW_BASE_URL=sandbox.flow.cl

# URL base de tu aplicación (para las URLs de confirmación y retorno)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Configuración de PayPal (existente)
NEXT_PUBLIC_PAYPAL_BUSINESS_EMAIL=tu-email@paypal.com
